import { Container, SettingsIcon, Title } from "./styles";

export const FuelComponent = () => {
  return (
    <Container>
      <Title>Posto ReactJS</Title>
      <SettingsIcon />
    </Container>
  );
};
